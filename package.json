{
  "name": "sonora-extension",
  "version": "0.7.1",
  "private": true,
  "description": "Extensão Chrome Sonora com React, TypeScript e arquitetura modular.",
  "scripts": {
    "clean": "node scripts/build.mjs --clean",
    "typecheck": "node scripts/typecheck.mjs",
    "build": "node scripts/build.mjs",
    "zip": "node scripts/build.mjs --zip"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
